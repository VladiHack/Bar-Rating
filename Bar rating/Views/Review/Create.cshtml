@model Bar_rating.Models.Review
@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4><span style="font-style: italic;">Review</span></h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="ReviewText" class="control-label"><span style="font-style: italic;">Review Text</span></label>
                <input asp-for="ReviewText" class="form-control" />
                <span asp-validation-for="ReviewText" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
@if (ViewBag.Message != "")
{
    <div>
        @ViewBag.Message
    </div>
}
<div>
    <a class="btn btn-primary" asp-action="Index" asp-route-id="@ViewBag.restaurantId">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

